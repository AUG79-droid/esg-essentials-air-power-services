export default function VisualGallery({items}){return <div className="visual-gallery">{items.map((item,index)=><figure className={'concept-visual tone-'+index%3} key={item.title}><figcaption><span>VISUAL {String(index+1).padStart(2,'0')}</span><h2>{item.title}</h2></figcaption><div className="concept-flow" role="img" aria-label={`${item.title}: ${item.labels.join(', ')}`}>{item.labels.map((label,i)=><div className="concept-node" key={label}><i>{String(i+1).padStart(2,'0')}</i><strong>{label}</strong>{i<item.labels.length-1&&<span aria-hidden="true">→</span>}</div>)}</div><p className="visual-alt">Text alternative: {item.labels.join(' → ')}.</p></figure>)}</div>}
